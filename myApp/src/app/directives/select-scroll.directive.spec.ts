import { SelectScrollDirective } from './select-scroll.directive';

describe('SelectScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectScrollDirective();
    expect(directive).toBeTruthy();
  });
});
